{
  "name": "delovrukah",
  "private": true,
  "scripts": {
    "postinstall": "pnpm dotenv -- pnpm prisma generate",
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "dotenv-cli": "^11.0.0",
    "eslint": "^9.38.0",
    "prettier": "^3.6.2",
    "prisma": "^6.18.0",
    "ts-node": "^10.9.2",
    "turbo": "^2.5.8",
    "typescript": "5.9.2"
  },
  "prisma": {
    "seed": "pnpm ts-node prisma/seed.ts"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0"
  }
}
